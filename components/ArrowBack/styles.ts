import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        width: 40,
        height: 40,
        borderWidth: 1,
        borderRadius: 80,
        alignItems: 'center',
        justifyContent: 'center',
        position: 'absolute',
        top: 30,
    },
    left: {
        left: 20
    },
    right: {
        right: 20
    }
})